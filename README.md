# DM Closing System™ — GitHub Pages MVP

Static, no-build MVP. Deploy the contents of this folder to the root of a GitHub Pages repository.

The browser diagnostic engine is deliberately heuristic and transparent; never put a private AI API key in GitHub Pages JavaScript. For production, replace that engine with a server-side endpoint using the same output contract.
