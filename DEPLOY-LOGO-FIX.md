# DM Closing System — logo/deployment fix

GitHub Pages publishes this repository from the repository root. The live repository stores `logo-circle.jpg` and `logo-rectangle.jpg` at the root, so both HTML files now reference the root logo files.

Replace the repository-root `index.html`, `analyzer.html`, `logo-circle.jpg`, and `logo-rectangle.jpg` with the files in this package. Do not place them inside a nested `site/` folder.
